{
  "name": "risk-of-catan",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "common",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build:common": "npm run build -w common",
    "watch:common": "npm run build -w common -- -w",
    "dev:backend": "npm run build:common && npm run dev -w backend",
    "dev:frontend": "npm run build:common && npm run start -w frontend",
    "build": "npm run build:common && npm run build -w backend && npm run build -w frontend",
    "pretty": "prettier --write \"**/src/**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.4.0"
  }
}
