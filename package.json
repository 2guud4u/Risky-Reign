{
  "name": "risky-reign",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "common",
    "backend",
    "ui"
  ],
  "scripts": {
    "build:common": "npm run build -w common",
    "watch:common": "npm run build -w common -- -w",
    "dev:backend": "npm run build:common && npm run dev -w backend",
    "dev:ui": "npm run build:common && npm run start -w ui",
    "build": "npm run build:common && npm run build -w backend && npm run build -w ui",
    "pretty": "prettier --write \"**/src/**/*.{ts,tsx}\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "typescript": "^5.4.0"
  }
}
